# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 0.0.1-alpha
maven_group = fr.firstmegagame4
archives_base_name = htgitn

# Dependencies are managed at gradle/libs.versions.toml
